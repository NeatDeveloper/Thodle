model LectorRank {
    id Int @id @default(autoincrement())

    title        String @unique
    shortVariant String @unique @map("short_variant")

    lectors Lector[]

    @@schema("amplua")
    @@map("LectorRanks")
}
